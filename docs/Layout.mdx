---
name: Layout
order: 85
---

import { Playground, Props } from 'docz';
import { LayoutProvider, useLayout, Text } from '../src';

export default ({ children }) => <LayoutProvider>{children}</LayoutProvider>;

# Layout

Paramount provides building blocks to create responsive layout via components `Visible`, `Row and Column`, `Container` and convenience functions ([see Usage](#usage))

## Setup

Make sure to use `LayoutProvider` at the top-level component. For options, [see Configuration Options](#options)

Use `debug` prop to see the layout

```tsx
<LayoutProvider value={{ layout: options }}>{children}</LayoutProvider>
```

## Usage

Paramount exposes a convenience hook `useLayout` that you can import to make use of utility functions for layout

### currentScreenSize

Retrieves the current screen size

<Playground>
  {() => {
    const { currentScreenSize } = useLayout();

    return (
      <Text>{`currentScreenSize: ${currentScreenSize}`}</Text>
    );

}}

</Playground>

### getResponsiveValue

<Playground>
  {() => {
    const { getResponsiveValue } = useLayout();
    const color = getResponsiveValue({
      xsmall: 'red',
      large: 'blue',
    });
    const number = getResponsiveValue({
      xsmall: 0,
      large: 1,
    });

    return <Text>{`color: ${color}, number: ${number}`}</Text>;

}}

</Playground>

### Responsive Components

`Visible`, `Row and Column`, `Container`

## Options

These are all the available configuration options, as well as the default layout configuration

```ts
export const defaultLayout: Layout = {
  breakpoints: {
    small: 480,

    medium: 768,

    large: 992,

    xlarge: 1200,
  },
  containerSizes: {
    small: 540,

    medium: 720,

    large: 960,

    xlarge: 1200,
  },
  gridColumnCount: 12,
  gutterWidth: 32,
};
```
