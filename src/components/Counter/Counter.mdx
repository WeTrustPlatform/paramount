---
name: Counter
menu: Components
---

import { Playground, Props } from 'docz';
import { Counter, State } from '..';

# Counter

## Usage

<Playground>
  <State initial={{ value: 0 }}>
    {({ state, setState }) => (
      <Counter
        value={state.value}
        onValueChange={value => setState({ value })}
      />
    )}
  </State>
</Playground>

## Props

<Props of={Counter} />
