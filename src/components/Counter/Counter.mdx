---
name: Counter
menu: Components
---

import { CounterState } from '../Helpers';
import { Playground, Props } from 'docz';
import { Counter } from '.';

# Counter

### Usage

<Playground>
  <CounterState initial={0}>
    {({ count, inc, dec }) => (
      <Counter
        count={count}
        onDecrement={dec}
        onIncrement={inc}
        min={0}
        max={10}
        component={null}
        getStyles={(props, theme) => ({
          containerStyle: {},
          counterStyle: {},
          countStyle: {},
          disabledStyle: {},
          ntextStyle: {},
        })}
      />
    )}
  </CounterState>
</Playground>

### Props

<Props of={Counter} />
