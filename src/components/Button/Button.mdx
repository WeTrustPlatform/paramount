---
name: Button
menu: Components
---

import { Playground, PropsTable } from 'docz';
import Button from './Button';
import { Box } from '../Layout';

# Button

### Usage

<Playground>
  <Button
    color="primary"
    appearance="primary"
    size="large"
    isLoading={false}
    isActive={false}
    isDisabled={false}
    isInline={false}
    iconBefore={null}
    iconAfter={null}
    iconLoading={null}
    title="Usage"
    onPress={() => console.log('Pressed!')}
    getStyles={(props, theme) => ({
      buttonStyle: {},
      textStyle: {},
      focusColor: '',
    })}
  />
</Playground>

### Primary appearance (default)

<Playground>
  <Button color="default" title="Default" />
  <Button color="primary" title="Primary" />
  <Button color="secondary" title="Secondary" />
  <Button color="danger" title="Danger" />
</Playground>

### Outline appearance

<Playground>
  <Button appearance="outline" color="default" title="Default" />
  <Button appearance="outline" color="primary" title="Primary" />
  <Button appearance="outline" color="secondary" title="Secondary" />
  <Button appearance="outline" color="danger" title="Danger" />
</Playground>

### Minimal appearance

<Playground>
  <Button appearance="minimal" color="default" title="Default" />
  <Button appearance="minimal" color="primary" title="Primary" />
  <Button appearance="minimal" color="secondary" title="Secondary" />
  <Button appearance="minimal" color="danger" title="Danger" />
</Playground>

### Loading Primary button

<Playground>
  <Button isLoading appearance="primary" color="primary" title="Default" />
  <Button isLoading appearance="outline" color="primary" title="Primary" />
  <Button isLoading appearance="minimal" color="primary" title="Secondary" />
</Playground>

### Loading Secondary button

<Playground>
  <Button isLoading appearance="primary" color="secondary" title="Default" />
  <Button isLoading appearance="outline" color="secondary" title="Primary" />
  <Button isLoading appearance="minimal" color="secondary" title="Secondary" />
</Playground>

### Other props

<Playground>
  <Box>
    <Button isInline color="primary" title="Inline" />
    <Button isDisabled color="primary" title="Disabled" />
  </Box>
</Playground>

If `isInline` does not inline the button, try wrapping it with `Box`

### Props

<PropsTable of={Button} />
