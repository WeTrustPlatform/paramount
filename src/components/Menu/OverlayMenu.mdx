---
name: OverlayMenu
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { Toggle } from 'react-powerplug';

import OverlayMenu from './OverlayMenu';
import { Button } from '../Button';
import { Box, Spacing } from '../Layout';
import { Text } from '../Typography';

## Usage

### OverlayMenus with fixed width and height content

<Playground>
  <Toggle>
    {({ on, toggle }) => (
      <>
        <Button onPress={toggle} isInline>
          Open overlay menu
        </Button>
        <OverlayMenu isVisible={on} onClose={toggle}>
          <Box>
            <Button onPress={toggle}>Open overlay menu</Button>
            <Button onPress={toggle}>Open overlay menu</Button>
            <Button onPress={toggle}>Open overlay menu</Button>
            <Spacing paddingTop={3}>
              <Button onPress={toggle}>Close</Button>
            </Spacing>
          </Box>
        </OverlayMenu>
      </>
    )}
  </Toggle>
</Playground>
