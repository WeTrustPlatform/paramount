---
name: DrawerMenu
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { Toggle } from 'react-powerplug';

import DrawerMenu from './DrawerMenu';
import { Button } from '../Button';
import { Box, Spacing } from '../Layout';
import { Text } from '../Typography';

## Usage

### DrawerMenus with fixed width and height content

<Playground>
  <Toggle>
    {({ on, toggle }) => (
      <>
        <Button onPress={toggle} isInline>
          Open overlay menu
        </Button>
        <DrawerMenu
          options={[
            {
              onPress: toggle,
              children: 'Menu Option 1',
              color: 'danger',
              appearance: 'minimal',
            },
            {
              onPress: toggle,
              children: 'Menu Option 2',
              color: 'primary',
              appearance: 'minimal',
            },
            {
              onPress: toggle,
              children: 'Menu Option 3',
              color: 'secondary',
              appearance: 'minimal',
            },
            {
              onPress: toggle,
              children: 'Menu Option 4',
              appearance: 'minimal',
            },
          ]}
          isVisible={on}
          onClose={toggle}
        />
      </>
    )}
  </Toggle>
</Playground>
