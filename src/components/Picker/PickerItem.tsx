import { Picker as RNPicker, PickerItemProps } from 'react-native';

const PickerItem = RNPicker.Item;

// tslint:disable-next-line
export interface PickerItemProps extends PickerItemProps {}

export default PickerItem;
