---
name: Badge
menu: Components
---

import { Playground, PropsTable } from 'docz';
import Badge from './Badge';
import { ThemeContext } from '../../theme';
import { Box } from '../Layout';

## Usage

**Note** Accessibility/Interactivity is a responsibility of a wrapping component

### Default

<Playground>
  <ThemeContext.Consumer>
    {theme => (
      <Box>
        {Object.keys(theme.fills.solid).map(color => (
          <Box flexDirection="row" key={color}>
            <Badge color={color}>{color}</Badge>
            <Badge color={color} isSolid>
              {color}
            </Badge>
          </Box>
        ))}
      </Box>
    )}
  </ThemeContext.Consumer>
</Playground>

### Pills

<Playground>
  <ThemeContext.Consumer>
    {theme => (
      <Box>
        {Object.keys(theme.fills.solid).map(color => (
          <Box flexDirection="row" key={color}>
            <Badge shape="pill" color={color}>
              {color}
            </Badge>
            <Badge shape="pill" color={color} isSolid>
              {color}
            </Badge>
          </Box>
        ))}
      </Box>
    )}
  </ThemeContext.Consumer>
</Playground>

## Props

<PropsTable of={Badge} />
