---
name: Switch
menu: Components
---

import { Playground, PropsTable } from 'docz';
import { Toggle } from 'react-powerplug';
import Switch from './Switch';
import { Box } from '../Layout';

# Switch

### Usage

<Playground>
  <Box>
    <Toggle initial={true}>
      {({ on, toggle }) => (
        <Switch
          isSwitchedOn={on}
          onChange={toggle}
          isDisabled={false}
          onIcon={null}
          offIcon={null}
          duration={300}
          getStyles={(props, theme) => ({
            circleStyle: {},
            containerStyle: {},
            backgroundColorOff: '#F5F6F7',
            backgroundColorOn: '#67c6bb',
            circleColorOff: 'white',
            circleColorOn: 'white',
          })}
        />
      )}
    </Toggle>
  </Box>
</Playground>

## Props

Extends `TouchableOpacityProps`. Read from `activeOpacity` and above for component specific props

<PropsTable of={Switch} />
