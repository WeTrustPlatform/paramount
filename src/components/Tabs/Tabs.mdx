---
name: Tabs
menu: Components
---

import { Playground, Props } from 'docz';

import { Icon, State, Tabs, Tab } from '..';

# Tabs

You can use a custom component in place of `Tab` component, though keep in mind you need to manage `isActive` and `onPress` by yourself

### Usage

<Playground>
  <State initial={{ activeTabIndex: 0 }}>
    {({ state, setState }) => (
      <Tabs
        activeTabIndex={state.activeTabIndex}
        onTabPress={index => setState({ activeTabIndex: index })}
      >
        {new Array(4).fill(0).map((v, i) => (
          <Tab key={i} title={`Tab ${i + 1}`} />
        ))}
      </Tabs>
    )}
  </State>
</Playground>

### Tabs Props

<Props of={Tabs} />

### Tab Props

Extends `ButtonProps`

<Props of={Tab} />

### Customization

Using `getStyles` prop

```tsx
TabsStyles {
  containerStyle: ViewStyle;
}

getStyles={(TabsProps, Theme) => TabsStyles}

TabStyles {
  containerStyle: ViewStyle;
  touchableStyle: ViewStyle;
  textStyle: TextStyle;
}

getStyles={(TabProps, Theme) => TabStyles}
```

Tabs Markup

```tsx
<View style={containerStyle}>{tabs}</View>
```

Tab Markup

```tsx
<View style={containerStyle}>
  <Button touchableStyle textStyle>
</View>
```
