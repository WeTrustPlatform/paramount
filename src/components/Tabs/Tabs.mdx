---
name: Tabs
menu: Components
---

import { Playground, Props } from 'docz';
import { State } from 'react-powerplug';
import { View } from 'react-native';

import { Icon } from '../Icon';
import { Tabs, Tab } from '.';

# Tabs

Passing `activeTabIndex` and `onPress` will alternate to controlled usage (manually set tab)

You can use a custom `Tab` component as long as it accepts `isActive`, `onPress` props

### Usage

<Playground>
  <Tabs
    defaultActiveTabIndex={1}
    activeTabIndex={undefined}
    onPress={undefined}
    distribution="fit" // or 'scrollable'
    getStyles={(props, theme) => ({
      containerStyle: {}, // Only applied when distribution="fit"
      tabContainerStyle: {},
      buttonStyle: {},
      textStyle: {},
      dividerStyle: {},
    })}
  >
    {new Array(4).fill(0).map((v, i) => (
      <Tab key={i} title={`Tab ${i + 1}`} />
    ))}
  </Tabs>
</Playground>

### Scrollable

Scrollable Tabs will also inline `Tab`

<Playground>
  <Tabs distribution="scrollable">
    {new Array(20).fill(0).map((v, i) => (
      <Tab key={i} title={`Tab ${i + 1}`} />
    ))}
  </Tabs>
</Playground>

### Props

<Props of={Tabs} />
